# Board follow-up (housekeeping)

Created `project.md` at Stage: charting and `git.md` documenting the current `w/node-bullet-tooltip` / cut-from `selective-client-sync` without switching branches. Added a Pending WORK.md entry linking [[investigation.md]] for the selection-only `planPatchDOM`/`patchDOM` fast path then O(1) SiteEntry child index; regenerated `.scratch/index.md`. No performance fix implemented.
