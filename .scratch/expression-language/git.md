# Expression Language — git

- **Project branch:** `w/tree2-semantics`
- **Cut from:** `selective-client-sync`
- **Notes:** Issue 32 ready: catalog `ref` / `owned` (partition of `child`). Prior: `IF`, `OUTER`. Cut from `selective-client-sync`.
