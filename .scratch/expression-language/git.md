# Expression Language — git

- **Project branch:** `w/tree2-semantics`
- **Cut from:** `selective-client-sync`
- **Notes:** Prefix combinator `IF` (same-input pullback). Prior: `OUTER` (fused Owned walk). Cut from `selective-client-sync`.
