# Deploying Gambol alongside WordPress (SSH access)

Status: historical — superseded. Gambol production runs on Azure App Service ([[doc/reference/deploy-azure.md]]). The cPanel host only transparently forwards `/ambit` to Azure ([[doc/reference/cpanel-transparent-proxy.md]]); it does not run .NET, a Gambol app process, or application data.
