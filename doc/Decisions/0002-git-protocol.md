# Git procedure lives in git-protocol

Agents used a human original branch and `w/` project branches (see [[doc/Decisions/0001-agent-git-hygiene.md]]). That model is retired. Canonical git procedure is [[.cursor/skills/git-protocol/SKILL.md]] (`dev`, `ready`, `master`). Other instructions point there and do not copy the steps.
