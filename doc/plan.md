# Plan

## Status

✅ Cloned Oxpecker TodoList example into Gambol/
✅ Renamed TodoList.fsproj to Gambol.fsproj
✅ Project builds successfully

## 1. Modify app from TodoList to outline viewer

### 1a. Update data model from Todo to Node

- Replace Todo type with Node type (uid, name, children, text)
- Update components to work with hierarchical node structure instead of flat list
