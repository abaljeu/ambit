# Summary goal

New Summary line:

`Give this repo one git procedure: ordinary commits on **dev**, merge to **ready** after **Agent-done**, squash to **master**; other instructions point at the skill and do not copy it.`

## Why it matches wait-what and CONTEXT

[[.agents/skills/wait-what/SKILL.md]] asks for a re-pitch with a little context, in ASD-STE100 Simplified Technical English, with ubiquitous language from [[CONTEXT.md]]. The old Summary named a “canonical” skill. That is a label, not a goal. The new line states the goal first (one git procedure for this repo) and then gives the three places: ordinary commits on **dev**, merge to **ready** after **Agent-done**, squash to **master**. Those words match [[CONTEXT.md]]. The line does not use original branch, project branch, or `w/`. **Manual approval** is not in the Summary. Merge and squash stay with the human per [[.cursor/skills/git-protocol/SKILL.md]]; the goal line keeps that as “after **Agent-done**” and does not copy the skill.

STE100: one line, simple verbs (Give, merge, squash, point, do not copy), no hard wrap.

## What changed

- [[plan/git-protocol/project.md]] — rewrote `Summary:`; set `Updated: 2026-09-02`; left `Stage: active`.
- This report. No other files.
