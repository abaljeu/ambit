# Summary goal rewrite

New Summary line:

Give the Browser a Graph that starts with only the Workspace Nodes needed for ROOT and restored navigation, grow residency only through explicit Load, and keep the Server Graph fully Resident and authoritative.

## Why this matches wait-what and CONTEXT

[[.agents/skills/wait-what/SKILL.md]] asks for a re-pitch with a little context, ASD-STE100 Simplified Technical English, and ubiquitous language from [[CONTEXT.md]]. The old Summary mixed a mechanism phrase with status ("spec ready-for-agent, implementation issues in flight"). Status is not a goal. This line re-pitches the destination from [[spec.md]] Problem/Solution: the Browser Graph is a small resident start, Load is the only way residency grows, and the Server Graph stays the full authority.

CONTEXT terms used: **Browser** (not Client), **Graph**, **Workspace Node**, **ROOT**, **Load**, **Resident**, **Server**. The line does not use lazy, hydrated, or Client.

## What changed

In [[project.md]]: replaced the Summary with the goal line above. Set `Updated: 2026-09-02`. Left `Stage: active`.
