# Linux server

Stage: chart
Summary: Compile the Server for one Linux, and specify that Linux setup (the machine and the setup the Server needs). The two halves stay separate. The main target is a fast-loading Azure server. A docker container is a second target and is not locked. The first research question, before any other ticket, is whether a Docker deploy is useful to these goals. Usefulness is not known. The docker container stays unlocked until that result is in. The destination is a spec to hand off and iterate on. A later pass in this project compiles the Server and does the Linux setup.
Updated: 2026-09-23
