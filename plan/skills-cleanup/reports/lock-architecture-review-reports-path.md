# Lock: architecture review reports path

Earlier locks stay. Stage stays `chart`. This closes the last leftover choice from [[plan/skills-cleanup/reports/unresolved-from-initial-chart.md]].

**Architecture review output** goes to `plan/<slug>/reports/`, same as other Gambol reports. Not OS temp HTML. Later: change [[.agents/skills/improve-codebase-architecture/SKILL.md]] from temp HTML + Tailwind CDN + `xdg-open` to a Markdown report under the current Project's `reports/` (or the Project being reviewed). This matches the reports-path lock.

This pass did not edit that skill.
