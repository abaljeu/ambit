# Lock: delete triage later

Earlier locks stay. Alan said delete triage. Keep QA is locked in [[plan/skills-cleanup/reports/lock-keep-qa.md]].

**Delete triage.** [[.agents/skills/triage/SKILL.md]] (GitHub labels, `#42`, PRs, `.out-of-scope/`) is not used here. Later: remove the skill and any pointers to it (gambol.mdc, Ask-Matt, ticket docs). Do not delete the skill or edit those files in this charting pass. This is not a change to the locked Status token `needs-triage`.

**Keep QA** is a separate lock. Do not delete QA with triage.

Later obedience punch list (do not do in this charting pass):

- Delete [[.agents/skills/triage/SKILL.md]] and companion files such as [[.agents/skills/triage/OUT-OF-SCOPE.md]].
- Remove `/triage` from Ask-Matt and any other skill or ticket-doc pointer. Drop it from indexes if listed.

This pass did not delete the skill or rewrite bodies.
