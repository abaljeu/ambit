# Summary goal re-pitch

## New Summary

Specify and implement a Prolog-like Expression language in Amble. The language extends path references with a left-to-right word pipeline over the Graph and yields Node, text, and number Answers for Find.

## Why it matches wait-what and CONTEXT

[[.agents/skills/wait-what/SKILL.md]] asks for a re-pitch with a little context, in ASD-STE100, with ubiquitous language from [[CONTEXT.md]]. The old Summary was ticket and branch status. The new line states the goal and gives the missing context: Amble already has path references; this Project specifies and implements Expression as a Prolog-like language over the Graph.

The words match [[CONTEXT.md]]:

- **Amble** is the embedded language. **Expression** is the predicate, not a “query” language.
- **Graph**, **Node**, and **Answer** (Node, text, or number) are the domain.
- **Find** is the command that searches the Graph; Expression feeds that consumer.

STE100: two short sentences, active verbs (`Specify`, `implement`, `extends`, `yields`), no ticket counts, no retired `w/` branch names.

The Destination on [[plan/expression-language/map.md]] and the opening of [[plan/expression-language/spec.md]] name the same goal: a Prolog-like Expression language over the Graph that extends path references with a left-to-right word pipeline, plus specification and implementation of that language.

## What changed

In [[plan/expression-language/project.md]]:

- Replaced the Summary (was: spec locked, tickets 15–26 on `w/expr`, Chapter 11 harness gap).
- Set `Updated: 2026-09-02`.
- Left `Stage: active` unchanged.

No other project files changed. Did not regenerate [[plan/index.md]].
