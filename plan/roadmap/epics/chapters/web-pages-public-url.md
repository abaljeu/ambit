# Chapter: Public URL

**Part of:** [[plan/roadmap/epics/create-and-publish-web-pages.md]]
**Blocked by:** [[html-codec.md]].

## Context

A person publishes web pages. Visitors open a public URL and read without the App. The public URL returns that HTML File body (same bytes the codec writes).

## Goal

Visitors open a public URL and receive the HTML File body. Same bytes the codec writes. No App.

## Required for done

- [ ] Visitors open a public URL and receive the HTML File body. Same bytes the codec writes. No App.

## Notes

Public URL has no owning Project yet.
